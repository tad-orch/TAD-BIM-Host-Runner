# Backend bind address and port
HOST=127.0.0.1
PORT=8080

# Data storage
DATA_DIR=./data
# SQLITE_DB_PATH=./data/app.db

# Request and polling timing
BRIDGE_REQUEST_TIMEOUT_MS=10000
POLL_INTERVAL_MS=2000
POLL_TIMEOUT_MS=120000

# Replace the example baseUrl and bearer placeholder with your own bridge details.
HOSTS_JSON=[{"id":"tad-bim-01","baseUrl":"http://127.0.0.1:3001","machineType":"revit-bridge","capabilities":["revit","acc"],"enabledTools":["revit_ping","revit_create_wall"],"headers":{"authorization":"Bearer REPLACE_WITH_BRIDGE_TOKEN"}}]

# Optional local model settings
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:14b
