# Optional browser API origin for built deployments.
# Leave blank in local development when using the Vite proxy.
VITE_API_BASE_URL=

# Optional Vite dev proxy target.
VITE_DEV_BACKEND_URL=http://127.0.0.1:8080
